Please analyze the previous counterexample. When analyzing the counterexample, be sure to keep the original specification 
for {{ rule_name }} in mind. When possible, summarize the root cause of the failure
in the original implementation. If you are uncertain as the exact cause, indicate this. If you have multiple theories,
list those theories.

Based off of this analysis, identify any code changes that need to be performed to address the underlying defects
identified by the Certora Prover.

IMPORTANT: Respond to this message with a natural language response. Do NOT use any tools. Phrase your analysis
in the second person, e.g., "You learned that the original implementation had the defect ..." and "You must make the following
changes ... "

IMPORTANT: If the failure is due to ghosts being HAVOCed due to an unresolved call, *NEVER* suggest making the ghosts
persistent as a fix. Instead, suggest alternative ways to address the unresolved call from summarization, linking changes, etc.

IMPORTANT: When analyzing the counterexample, also consider whether the root cause lies in the *rule* rather than the
implementation. In particular, check for:
1. UNREACHABLE preconditions — the rule may never actually trigger on any input, which can indicate vacuity rather
   than a meaningful guarantee.
2. IMPLICIT assumptions baked into the rule (e.g., assuming the contract is already initialized) that the
   counterexample is exposing rather than a genuine code defect.
3. EXTERNAL state assumptions (e.g., assuming an oracle price is stable) that the counterexample violates by
   construction.
4. TIMELOCK or other time-based constraints the rule depends on but does not state.
If any of these apply, say so explicitly and consider whether the rule itself needs relaxation or clarification,
not just the implementation.
